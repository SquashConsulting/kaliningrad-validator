/**
 * Validates given graph data structure
 *
 * @param data Kaliningrad Graph Structure
 * @returns `true` or `false`
 */
export default function validate(data: object): PromiseLike<any> | boolean;
